{
  "name": "hooman-watch-gallery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static Persian/RTL catalogue site for Hooman Watch Gallery, Ekbatan, Tehran",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check && astro build && npm run guard",
    "build:only": "astro build",
    "preview": "astro preview",
    "check": "node scripts/check-rtl.mjs && node scripts/check-links.mjs && node scripts/check-fonts.mjs",
    "guard": "node scripts/check-external.mjs && node scripts/check-internal-links.mjs",
    "test": "node --test 'src/**/*.test.js'",
    "verify": "npm run test && npm run build"
  },
  "dependencies": {
    "astro": "7.2.6",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "@ibm/plex-mono": "2.5.0",
    "@ibm/plex-sans": "1.1.0",
    "@ibm/plex-sans-arabic": "1.1.0",
    "@tailwindcss/vite": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
